spring:
  datasource:
    url: jdbc:mysql://localhost:3306/metro?createDatabaseIfNotExist=true
    username: root
    password: pass
  jpa:
    hibernate:
      ddl-auto: create
      
# eureka:
#   client:
#     service-url:
#       defaultZone: http://localhost:8761/eureka/

server:
  port: 0
